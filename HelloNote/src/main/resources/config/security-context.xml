<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<!-- security context Login 설정 -->
	<security:http pattern="/css/**" security="none" />
	<security:http>
		<security:intercept-url method="GET"
			pattern="/login.jsp" access="permitAll()" />
		<security:intercept-url method="POST"
			pattern="/processLogin" access="permitAll()" />
		<!-- ALL -->
		<security:intercept-url pattern="/top.jsp"
			access="permitAll()" />
		<!-- admin -->
		<security:intercept-url pattern="/admin/**"
			access="hasAuthority('M')" />
		<!-- user -->
		<security:intercept-url pattern="/**"
			access="isAuthenticated()" />

		<security:form-login login-page="/login.jsp"
			login-processing-url="/processLogin" default-target-url="/top.jsp"
			authentication-failure-url="/login.jsp"
			password-parameter="paramPassword" username-parameter="paramLoginId" />

		<security:logout logout-url="/logout"
			logout-success-url="/top.jsp" />
		<security:csrf disabled="true" />
	</security:http>

	<!-- user 정보 -->
	<security:authentication-manager>
		<security:authentication-provider>
			<!-- <security:user-service> <security:user name="user" password="1234" 
				authorities="ROLE_USER" /> <security:user name="admin" password="1234" authorities="ROLE_ADMIN" 
				/> </security:user-service> -->
			<!-- Oracle DB 에서 인증받아옴 -->
			<security:jdbc-user-service
				data-source-ref="dataSource"
				users-by-username-query="select HELLONOTE_ID, PW, 1
										 from HELLONOTE_USERS
										 where HELLONOTE_ID = ?"
				authorities-by-username-query="select HELLONOTE_ID, H_GRANT
											   from HELLONOTE_USERS
											   where HELLONOTE_ID = ?" />
		</security:authentication-provider>
	</security:authentication-manager>

</beans>
